# PostgreSQL Upgrade Role

This role automates the process of upgrading PostgreSQL from an older version to a newer version.

## Directory Structure

- playbooks/: Contains the main playbook.
- roles/: Contains the role-specific files and tasks.

## Usage

Run the playbook:
ansible-playbook playbooks/upgrade_main.yml -i inventory.yml
